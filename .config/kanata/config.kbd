(defsrc
  a s d f j k l ;
)

(defalias
  a-lsup (tap-hold-release 200 300 a lmet)
  s-lalt (tap-hold-release 200 200 s lalt)
  d-lsft (tap-hold-release 200 200 d lsft)
  f-lctl (tap-hold-release 200 200 f lctl)

  j-rctl (tap-hold-release 200 200 j rctl)
  k-rsft (tap-hold-release 200 200 k rsft)
  l-ralt (tap-hold-release 200 200 l ralt)
  ;-rsup (tap-hold-release 200 300 ; rmet)
)

(deflayer base
  @a-lsup @s-lalt @d-lsft @f-lctl @j-rctl @k-rsft @l-ralt @;-rsup
)
